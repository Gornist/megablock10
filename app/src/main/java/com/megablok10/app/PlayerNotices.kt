package com.megablok10.app

/**
 * Короткое сообщение игроку поверх любого экрана («Код уже использован», «Игрок с другой версией приложения»). Доменный код
 * сообщает через этот интерфейс и не знает, как это показано; реализация — снэкбар интерфейса (ui.theme.AppSnack), её
 * подставляет корень композиции (di.AppGraph), в тестах — список строк.
 */
fun interface PlayerNotices {
    fun show(text: String)
}
