/**
 * Интервалы polling для useApiData/usePolledData — раньше разбросаны магическими
 * числами по экранам (3000/5000/8000/10000/15000/30000 без единого источника),
 * что затрудняло понять, какой экран насколько "горячий" и почему именно так.
 * Не привязка к конкретному экрану, а к тому, насколько свежими должны быть данные.
 */
export const POLL_LIVE_MS = 3000; // Обзор: счётчики и живая лента — то, на что мастер смотрит не отрываясь
export const POLL_DEFAULT_MS = 8000; // дефолт useApiData — большинство списковых экранов без явного pollMs
export const POLL_MODERATE_MS = 5000; // события, объявления — читают, а не следят непрерывно
export const POLL_RELAXED_MS = 10000; // "Требует внимания", выдача QR персонажа — секунды не критичны
export const POLL_BACKGROUND_MS = 15000; // список "под наблюдением" — справочный, обновляется редко
export const POLL_SLOW_MS = 30000; // агрегированная метрика (пульс за час) — секунды заведомо не важны
